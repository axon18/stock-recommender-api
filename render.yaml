
# render.yaml: configuration file for Render deployment
render_yaml_code = '''
services:
  - type: web
    name: stock-recommendation-api
    env: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
'''

# Write the files to disk
with open("main.py", "w") as f:
    f.write(main_code)

with open("requirements.txt", "w") as f:
    f.write(requirements_code)

with open("render.yaml", "w") as f:
    f.write(render_yaml_code)

print("Files main.py, requirements.txt, and render.yaml have been created for Render deployment.")
